
i jave pulled the code file from mumbai server to singapore server
now i am writing continuation code

// Java Program to Swap Two values using third variable
// using temp variable
 
// Importing generic libraries
import java.util.*;
 
class GFG {
 
    // Function to swap two numbers
    // Using temporary variable
    static void swapValuesUsingThirdVariable(int m, int n)
    {
        // Swapping the values
        int temp = m;
        m = n;
        n = temp;
        System.out.println("Value of m is " + m
                           + " and Value of n is " + n);
    }
 
now i will push this code to centrol repo from singapore server to mumbai server

